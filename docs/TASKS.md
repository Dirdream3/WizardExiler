# 任务板

> ★ **这是多人 / 多 AI 并行时唯一的协调机制** ★
> 动手之前把任务状态改成 `🔨 进行中` 并写上自己是谁（`@某人` / `@claude` / `@cursor`），
> 做完改成 `✅ 完成`。**别跳过这一步** —— 两个 AI 同时改同一个文件，冲突了很难拆。

状态：`📋 待办` `🔨 进行中` `✅ 完成` `🧊 搁置`

---

## 进行中

| 状态 | 任务 | 负责人 | 主要文件 |
|---|---|---|---|
| —— | 目前没有人在做 | | |

---

## 待办

按"改动范围"分组。**同一组里的任务会互相冲突，不要并行；不同组之间基本安全。**

### A 组 · 内容数据 ← 最适合并行、最适合新人

只改 `data/`，一行逻辑都不用写，加完自动出现在背包里（老存档也会自动补进来）。

| 状态 | 任务 | 主要文件 |
|---|---|---|
| 📋 | 再加几颗辅助宝石（暴击伤害 / 附加冰霜 / 减速） | `data/gem_library.gd` |
| 📋 | 再加几件装备（胸甲 2×3、腰带 3×1、护符 1×1） | `data/equip_library.gd` |
| 📋 | 加第三、第四个主动技能石（近战、范围） | `data/gem_library.gd` |

### B 组 · 战斗机制

| 状态 | 任务 | 主要文件 | 备注 |
|---|---|---|---|
| 📋 | 技能冷却接进施法流程 | `game/player.gd` | `SkillSpec.base_cooldown` 字段早就留好了，还没接 |
| 📋 | 范围技能（新星 / 爆炸），把 `AREA_OF_EFFECT` 用起来 | `combat/` `data/` | 这条属性现在没人用（见 ADR-011） |
| 📋 | 更多异常状态：冰缓、易伤 | `data/demo_content.gd` | 照着 `buff_shock` 抄 |

### C 组 · 背包 / 装备

| 状态 | 任务 | 主要文件 | 备注 |
|---|---|---|---|
| 📋 | 更多装备形状（L 形、T 形），让摆放取舍更大 | `combat/gem_shape.gd` | 现在只有矩形 |
| 📋 | 装备也能升级 | `combat/equip_item.gd` | 现在 `max_level = 1`，`[-]/[+]` 对装备无效 |
| 📋 | 宝石经验 / 品质 | `combat/skill_gem.gd` | |
| 📋 | 拖拽放置（现在是"点一下拿起、再点一下放下"） | `game/gem_grid_view.gd` | 现在的方式更不容易出 bug，改之前想清楚值不值 |

### D 组 · 掉落与成长

| 状态 | 任务 | 主要文件 | 备注 |
|---|---|---|---|
| 📋 | 怪物掉落宝石 / 装备 | `game/enemy.gd` `game/world.gd` | 掉落物要能进背包 |
| 📋 | 随机词缀生成（词缀池 + 随机词条） | `data/` | 复用 `Modifier` 和 `remove_by_source` |

### E 组 · 表现与工程

| 状态 | 任务 | 主要文件 | 备注 |
|---|---|---|---|
| 📋 | 换真美术：行走动画、TileMap 地图 | `game/pixel_art.gd` | 换掉 `sprite.texture` 即可，其它代码不用动 |
| 📋 | 词缀改成 CSV / Excel 导表 | `data/` | 宝石上百颗之后手写不现实 |
| 📋 | GitHub Actions：push 时自动跑两套测试 | `.github/workflows/` | 需要先能推到 GitHub |

---

## 已完成

| 任务 | ADR |
|---|---|
| ✅ 战斗核心：词缀 / 标签 / Buff / 五步伤害管线 | ADR-001 ~ 003 |
| ✅ 投射物：穿透 / 分叉 / 弹射 / 撞墙反弹 / 散射 / 乱窜 | |
| ✅ 技能石系统（等级 / 成长 / 标签 / 辅助宝石） | ADR-004 |
| ✅ 「电球术」= Spark 的命名修正 | ADR-005 |
| ✅ 背包乱斗式网格：箭头指着谁就辅助谁 | ADR-006 |
| ✅ 左侧常驻面板 + 右侧 400×400 正方形战斗画面（SubViewport） | ADR-007、008 |
| ✅ 背包存档（JSON，四层容错） | ADR-009 |
| ✅ 装备系统（同网格，放着就生效） | ADR-010、012 |
| ✅ 删除脉冲机制和「闪电球」 | ADR-011 |
| ✅ 协作文档（AGENTS / CONTRIBUTING / ARCHITECTURE / DECISIONS / TASKS） | ADR-015 |

---

## 认领示例

```markdown
| 🔨 | 再加几颗辅助宝石 | @claude | `data/gem_library.gd` |
```

做完记得：

1. 两套测试全绿（[CONTRIBUTING.md](../CONTRIBUTING.md#2-跑测试)）
2. 把这里的状态改成 ✅，挪到「已完成」
3. 有新的设计决定 → 追加一条 ADR 到 [DECISIONS.md](DECISIONS.md)
